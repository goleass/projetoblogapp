

<div style="width: 500px; margin: 100px auto; margin-top: 20px;" class="col-md-5 col-sm-5 col-12">
{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
    <h3>Nova Categoria:</h3>
    <div class="card">
        <div class="card-body">
            <form action="/admin/categorias/nova" method="POST">
                <label for="nome">Nome: </label>
                <input type="text" id="nome" name="nome" placeholder="Nome da categoria" class="form-control" required>
                <label for="slug">Slug: </label>
                <input style="float: left" type="text" id="slug" name="slug" placeholder="Slug da categoria" class="form-control" required></br></br></br>
                <button style="float: right" class="btn btn-success">criar categoria</button>
            </form>
        </div>
    </div>
</div>